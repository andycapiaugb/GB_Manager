<h1>New Diploma</h1>
<%= form_for @diploma do |f| %>
    <div class="field">
      <%= f.label :diploma_degree %>
      <%= f.select(:diploma_degrees_id, Degree.all.collect{|dd| [dd.degree, dd.id]}) %>
    </div>
    <div class="field">
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>
    <div class="actions">
      <%= f.submit :save %>
    </div>
<% end %>